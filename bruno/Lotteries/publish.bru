meta {
  name: Publish Lottery
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/lotteries/{{lotteryId}}/publish
  body: none
  auth: bearer
}

headers {
  Authorization: Basic {{accessToken}}
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  lotteryId: 1
}
