meta {
  name: Get Ticket by ID
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/tickets/{{ticketId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  ticketId: 4
}
